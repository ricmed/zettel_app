# ── Core ────────────────────────────────────────────────────────────────
typer[all]>=0.9.0
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9
rich>=13.0.0
pydantic>=2.0.0
pyyaml>=6.0
python-ulid>=2.0.0
python-dotenv>=1.0.0

# ── LLM / LangChain ────────────────────────────────────────────────────
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-openai>=0.2.0          # Provider: OpenAI (default)
langgraph>=0.2.0                 # Article pipeline StateGraph
litellm>=1.40.0                  # Pricing map only (cost_per_token); not used as LLM client
# langchain-anthropic>=0.3.0     # Provider: Anthropic (descomente se usar)
# langchain-google-genai>=2.0.0  # Provider: Gemini (descomente se usar)
# langchain-ollama>=0.2.0        # Provider: Ollama local (descomente se usar)

# ── Vector Store ────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── PDF Extraction ──────────────────────────────────────────────────────
docling>=2.0.0                   # Extrator principal de PDF
# pymupdf>=1.24.0               # Fallback para PDF (descomente se preferir)

# ── Clustering / MOCs (opcional) ────────────────────────────────────────
numpy>=1.24.0
scikit-learn>=1.3.0
# umap-learn>=0.5.0             # Descomente para clusterização avançada
# hdbscan>=0.8.0                # Descomente para clusterização avançada
langchain-core
langchain-openai
langchain-text-splitters
numpy
pydantic
python-dotenv
python-ulid
pyyaml
rich
scikit-learn
torch
typer[all]
pytest
PyMuPDF>=1.24.0
